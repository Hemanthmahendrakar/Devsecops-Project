apiVersion: v1
kind: Service

metadata:
  name: expense-service
  namespace: expense-system

spec:
  selector:
    app: expense-tracker

  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000

  type: ClusterIP