apiVersion: v1
kind: Service

metadata:
  name: digital-twin-service
  namespace: expense-system

spec:
  selector:
    app: digital-twin

  ports:
    - protocol: TCP
      port: 5001
      targetPort: 5001

  type: ClusterIP